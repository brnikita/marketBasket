define(
    ['backbone', './models'],
    function (Backbone, Models) {
        var Collections = {
            Collection: Backbone.Collection.extend({

            })
        };

        return Collections;
    });
